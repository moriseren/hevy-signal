name: Publish optimized v3.2 (verified)

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  publish:
    if: github.head_ref == 'publish-v3.2-final'
    runs-on: macos-14
    env:
      GH_TOKEN: ${{ github.token }}
      EXPECTED_SOURCE_SHA256: 93077dbd213ba86a3bfeab46d3299439085eb2c4a3dbe8dcab109077c9127136
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.x'

      - name: Restore and verify optimized source
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import base64, glob, hashlib, pathlib
          parts = sorted(glob.glob('release-staging/optimized.part*.b64'))
          if len(parts) != 4:
              raise SystemExit(f'expected 4 optimized source chunks, found {len(parts)}')
          encoded = ''.join(pathlib.Path(p).read_text().strip() for p in parts)
          raw = base64.b64decode(encoded, validate=True)
          actual = hashlib.sha256(raw).hexdigest()
          expected = '93077dbd213ba86a3bfeab46d3299439085eb2c4a3dbe8dcab109077c9127136'
          if actual != expected:
              raise SystemExit(f'source checksum mismatch: {actual}')
          pathlib.Path('Hevy-Signal-v3.2-source.zip').write_bytes(raw)
          print(f'optimized source verified: {actual}')
          PY
          unzip -t Hevy-Signal-v3.2-source.zip
          mkdir -p build-src
          unzip -q Hevy-Signal-v3.2-source.zip -d build-src
          test -f build-src/Hevy-Signal-v3.2-source/main.go

      - name: Test source
        working-directory: build-src/Hevy-Signal-v3.2-source
        run: go test main.go main_test.go

      - name: Build Windows and macOS packages
        shell: bash
        run: |
          set -euo pipefail
          SRC="$PWD/build-src/Hevy-Signal-v3.2-source"
          DIST="$PWD/dist"
          mkdir -p "$DIST/windows-x64" "$DIST/windows-arm64" "$DIST/windows-all/windows-x64" "$DIST/windows-all/windows-arm64"

          cd "$SRC"
          GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -trimpath -ldflags='-s -w -H=windowsgui' -o "$DIST/windows-x64/HevySignal.exe" main.go
          GOOS=windows GOARCH=arm64 CGO_ENABLED=0 go build -trimpath -ldflags='-s -w -H=windowsgui' -o "$DIST/windows-arm64/HevySignal.exe" main.go
          cp "$DIST/windows-x64/HevySignal.exe" "$DIST/windows-all/windows-x64/HevySignal.exe"
          cp "$DIST/windows-arm64/HevySignal.exe" "$DIST/windows-all/windows-arm64/HevySignal.exe"

          for d in "$DIST/windows-x64" "$DIST/windows-arm64" "$DIST/windows-all"; do
            cat > "$d/README-FIRST.txt" <<'EOF'
          Hevy Signal v3.2 Public Beta

          Unzip, then run HevySignal.exe from the folder matching your Windows architecture.
          Use your own Hevy Pro API key for automatic sync, or Import Hevy CSV for fully local analysis without Hevy Pro.
          No OpenAI API key is required. Independent project; not affiliated with or endorsed by Hevy.
          EOF
          done

          APP="$DIST/Hevy Signal.app"
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
          GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' -o "$APP/Contents/Resources/HevySignalBackend" main.go
          GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' -o "$APP/Contents/MacOS/HevySignalLauncher" cmd/macos-launcher/main.go
          chmod 755 "$APP/Contents/MacOS/HevySignalLauncher" "$APP/Contents/Resources/HevySignalBackend"
          cat > "$APP/Contents/Info.plist" <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>CFBundleExecutable</key><string>HevySignalLauncher</string>
            <key>CFBundleIdentifier</key><string>com.hevysignal.local</string>
            <key>CFBundleName</key><string>Hevy Signal</string>
            <key>CFBundleDisplayName</key><string>Hevy Signal</string>
            <key>CFBundlePackageType</key><string>APPL</string>
            <key>CFBundleShortVersionString</key><string>3.2</string>
            <key>CFBundleVersion</key><string>3.2</string>
            <key>LSMinimumSystemVersion</key><string>11.0</string>
            <key>LSArchitecturePriority</key><array><string>arm64</string></array>
          </dict></plist>
          EOF
          plutil -lint "$APP/Contents/Info.plist"
          file "$APP/Contents/MacOS/HevySignalLauncher" "$APP/Contents/Resources/HevySignalBackend"

          cd "$DIST/windows-x64" && zip -q -r "$DIST/Hevy-Signal-v3.2-Windows-x64.zip" .
          cd "$DIST/windows-arm64" && zip -q -r "$DIST/Hevy-Signal-v3.2-Windows-ARM64.zip" .
          cd "$DIST/windows-all" && zip -q -r "$DIST/Hevy-Signal-v3.2-Windows-all.zip" .
          cd "$DIST" && ditto -c -k --sequesterRsrc --keepParent 'Hevy Signal.app' Hevy-Signal-v3.2-macOS-Apple-Silicon.zip
          cp "$GITHUB_WORKSPACE/Hevy-Signal-v3.2-source.zip" "$DIST/Hevy-Signal-v3.2-source.zip"
          cd "$DIST"
          shasum -a 256 Hevy-Signal-v3.2-Windows-all.zip Hevy-Signal-v3.2-Windows-x64.zip Hevy-Signal-v3.2-Windows-ARM64.zip Hevy-Signal-v3.2-macOS-Apple-Silicon.zip Hevy-Signal-v3.2-source.zip > Hevy-Signal-v3.2-SHA256.txt

      - name: Publish v3.2 release
        shell: bash
        run: |
          set -euo pipefail
          if ! gh release view v3.2 >/dev/null 2>&1; then
            gh release create v3.2 --target main --title 'Hevy Signal v3.2 Public Beta' --notes 'Hevy Signal v3.2 Public Beta adds fully local Hevy CSV import without Hevy Pro, keeps optional Hevy Pro API sync, and includes the optimized CSV parser (~3.4x faster in the 60k-set benchmark with ~95% lower transient allocation volume).'
          fi
          gh release upload v3.2 \
            dist/Hevy-Signal-v3.2-Windows-all.zip \
            dist/Hevy-Signal-v3.2-Windows-x64.zip \
            dist/Hevy-Signal-v3.2-Windows-ARM64.zip \
            dist/Hevy-Signal-v3.2-macOS-Apple-Silicon.zip \
            dist/Hevy-Signal-v3.2-source.zip \
            dist/Hevy-Signal-v3.2-SHA256.txt \
            --clobber

      - name: Verify release assets
        shell: bash
        run: |
          set -euo pipefail
          release="$(gh api repos/${{ github.repository }}/releases/tags/v3.2)"
          for name in \
            Hevy-Signal-v3.2-Windows-all.zip \
            Hevy-Signal-v3.2-Windows-x64.zip \
            Hevy-Signal-v3.2-Windows-ARM64.zip \
            Hevy-Signal-v3.2-macOS-Apple-Silicon.zip \
            Hevy-Signal-v3.2-source.zip \
            Hevy-Signal-v3.2-SHA256.txt; do
            echo "$release" | jq -e --arg n "$name" '.assets[] | select(.name == $n and .state == "uploaded")' >/dev/null
          done
          digest="$(echo "$release" | jq -r '.assets[] | select(.name == "Hevy-Signal-v3.2-source.zip") | .digest // empty')"
          test "$digest" = "sha256:$EXPECTED_SOURCE_SHA256"
          gh release view v3.2 --json tagName,name,isDraft,isPrerelease,assets
